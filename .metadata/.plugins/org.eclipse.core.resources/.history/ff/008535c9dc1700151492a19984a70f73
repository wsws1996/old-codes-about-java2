import java.net.*;


public class UDPSend {
	//1.创建UDP服务
	DatagramSocket ds=new DatagramSocket();
	//确定数据并封包
	byte [] data="connection succeeeding".getBytes();
	DatagramPacket dp=new DatagramPacket(data, data.length,InetAddress.getByName("王爽"), 10000);
	//发包
	ds.send(dp);
}
